using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public abstract class APlayerComponent : MonoBehaviour
{
    /*protected PlayerStats playerStats;
    void Start()
    {
        playerStats = GetComponent<PlayerService>().playerStats;
        if (playerStats == null)
            Debug.LogError("Player Stats not loaded at component: " + GetType().ToString());
    }*/
}
